{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "build": {
      "commands": [
        "...",
        "mkdir -p bin && printf '%s\\n' '#!/bin/sh' 'exec sh /app/start.sh' > bin/node && chmod 755 bin/node start.sh"
      ]
    }
  },
  "deploy": {
    "startCommand": "sh start.sh",
    "paths": ["/app/bin"]
  }
}
